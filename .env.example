# API Configuration
# For mock server (default with `pnpm dev`)
VITE_API_BASE_URL=http://localhost:8080/api/v1

# Note: When using `pnpm dev:real`, the VITE_USE_REAL_API flag
# automatically configures the API to use http://localhost:9000/api/v1
# (nginx CORS proxy to ArgoCD)
