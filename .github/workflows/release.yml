name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Install Helm
        uses: azure/setup-helm@v4
        with:
          version: 'latest'

      - name: Package Helm chart
        run: |
          helm package chart/ --destination .
          CHART_VERSION=$(helm show chart chart | grep '^version:' | awk '{print $2}')
          echo "CHART_VERSION=$CHART_VERSION" >> "$GITHUB_ENV"

      - name: Generate changelog
        id: changelog
        run: |
          # Get the previous tag
          PREVIOUS_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")

          # Generate changelog
          if [ -z "$PREVIOUS_TAG" ]; then
            echo "CHANGELOG=Initial release" >> "$GITHUB_OUTPUT"
          else
            CHANGELOG=$(git log "${PREVIOUS_TAG}..HEAD" --pretty=format:"- %s (%h)" --no-merges)
            # Escape newlines for GitHub output
            {
              echo "CHANGELOG<<EOF"
              echo "$CHANGELOG"
              echo "EOF"
            } >> "$GITHUB_OUTPUT"
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          body: |
            ## Cased CD ${{ github.ref_name }}

            A modern, beautiful UI for ArgoCD - Community Edition

            ### Installation

            **Using Helm:**
            ```bash
            helm repo add cased https://raw.githubusercontent.com/${{ github.repository }}/gh-pages
            helm repo update
            helm install cased-cd cased/cased-cd --version ${{ env.CHART_VERSION }}
            ```

            **Using Docker:**
            ```bash
            docker pull ghcr.io/${{ github.repository_owner }}/cased-cd:${{ github.ref_name }}
            ```

            **Enterprise Edition:**
            For advanced features including RBAC management, user management, audit trail, and notifications, visit [https://cased.com](https://cased.com)

            ### What's Changed

            ${{ steps.changelog.outputs.CHANGELOG }}

            ---

            **Documentation**: [README.md](https://github.com/${{ github.repository }}/blob/${{ github.ref_name }}/README.md)

            **Troubleshooting**: [TROUBLESHOOTING.md](https://github.com/${{ github.repository }}/blob/${{ github.ref_name }}/TROUBLESHOOTING.md)

            **Built by [Cased](https://cased.com)**
          files: |
            cased-cd-*.tgz
          draft: false
          prerelease: ${{ contains(github.ref_name, 'alpha') || contains(github.ref_name, 'beta') || contains(github.ref_name, 'rc') }}
